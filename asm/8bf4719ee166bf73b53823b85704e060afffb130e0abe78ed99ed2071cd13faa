 .name fcn.000d5e70
 .offset 00000000000d5e70
 .file bash
 push rbp
 mov edi, CONST
 push rbx
 sub rsp, CONST
 call CONST
 mov rbp, qword [rip + CONST]
 mov qword [rip + CONST], rax
 mov rdi, rbp
 call CONST
 lea rdi, qword [rax + CONST]
 call CONST
 mov rbx, qword [rip + CONST]
 mov rsi, rbp
 mov rdi, rax
 call CONST
 mov rdx, qword [rip + CONST]
 mov qword [rbx], rax
 mov rax, qword [rip + CONST]
 mov qword [rax + CONST], CONST
 mov qword [rax + CONST], rdx
 add rsp, CONST
 pop rbx
 pop rbp
 ret
