 .name fcn.00002b70
 .offset 0000000000002b70
 .file test
 movsxd rdx, dword [rip + CONST]
 mov rax, qword [rip + CONST]
 push rbp
 mov rdi, qword [rax + rdx*CONST + CONST]
 call CONST
 lea rsi, qword [rip + CONST]
 xor edi, edi
 mov edx, CONST
 mov rbp, rax
 call CONST
 mov rsi, rbp
 mov rdi, rax
 xor eax, eax
 call CONST
 nop dword [rax]
