 .name fcn.0008b310
 .offset 000000000008b310
 .file bash
 sub rsp, CONST
 mov edi, edi
 xor ecx, ecx
 mov esi, CONST
 mov dword [rip + CONST], CONST
 mov rdx, qword [rip + CONST]
 call CONST
 mov rdx, qword [rip + CONST]
 or dword [rax + CONST], CONST
 mov qword [rax], rdx
 mov qword [rip + CONST], rax
 xor eax, eax
 add rsp, CONST
 ret
