 .name fcn.0001bb90
 .offset 000000000001bb90
 .file dpkg
 endbr64
 push rbp
 push rbx
 mov rbx, rdi
 mov edi, CONST
 sub rsp, CONST
 call CONST
 mov rdi, qword [rbx + CONST]
 call CONST
 test eax, eax
 cjmp LABEL10
 add rsp, CONST
 pop rbx
 pop rbp
 ret
LABEL10:
 mov rbp, qword [rbx + CONST]
 lea rsi, [rip + CONST]
 mov edx, CONST
 lea rdi, [rip + CONST]
 call CONST
 mov rdi, rax
 mov rsi, rbp
 xor eax, eax
 call CONST
