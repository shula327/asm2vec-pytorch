 .name sym.Perl_cv_get_call_checker_flags
 .offset 000000000004e770
 .file perl5.30.0
 endbr64
 push r14
 mov r14, rsi
 push r13
 mov r13, rcx
 push r12
 mov r12, r8
 push rbp
 mov rbp, r9
 push rbx
 mov ebx, edx
 test dword [rsi + CONST], CONST
 cjmp LABEL12
LABEL28:
 and ebx, CONST
 lea rax, [rip + CONST]
 mov qword [r13], rax
 mov qword [r12], r14
 mov dword [rbp], ebx
 pop rbx
 pop rbp
 pop r12
 pop r13
 pop r14
 ret
LABEL12:
 mov esi, CONST
 mov rdi, r14
 call CONST
 test rax, rax
 cjmp LABEL28
 mov rdx, qword [rax + CONST]
 mov qword [r13], rdx
 mov rdx, qword [rax + CONST]
 mov qword [r12], rdx
 movzx edx, byte [rax + CONST]
 or ebx, edx
 and ebx, CONST
 mov dword [rbp], ebx
 pop rbx
 pop rbp
 pop r12
 pop r13
 pop r14
 ret
