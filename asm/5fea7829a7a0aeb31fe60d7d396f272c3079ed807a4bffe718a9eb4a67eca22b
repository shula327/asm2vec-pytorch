 .name fcn.00016090
 .offset 0000000000016090
 .file find
 push r13
 lea rdi, qword [rip + CONST]
 push r12
 push rbp
 push rbx
 sub rsp, CONST
 call CONST
 test rax, rax
 cjmp LABEL8
 mov rbp, rax
 xor r13d, r13d
 mov r12d, CONST
 call CONST
 mov rbx, rax
 nop dword [rax + rax]
LABEL23:
 mov dword [rbx], CONST
 mov rdi, rbp
 call CONST
 test rax, rax
 cjmp LABEL19
 cmp byte [rax + CONST], CONST
 cjmp LABEL21
 cmp byte [rax + CONST], CONST
 cjmp LABEL23
 cmp word [rax + CONST], CONST
 cjmp LABEL23
LABEL21:
 xor esi, esi
 lea rdi, qword [rax + CONST]
 mov r13d, CONST
 call CONST
 cmp r12d, eax
 cmovl r12d, eax
 jmp LABEL23
LABEL19:
 mov eax, dword [rbx]
 test eax, eax
 cjmp LABEL35
 mov rdi, rbp
 call CONST
 test r13d, r13d
 cjmp LABEL8
 add rsp, CONST
 mov eax, r12d
 pop rbx
 pop rbp
 pop r12
 pop r13
 ret
LABEL35:
 lea rdx, qword [rip + CONST]
 mov esi, CONST
 xor edi, edi
 call CONST
 mov esi, dword [rbx]
 xor edi, edi
 lea rdx, qword [rip + CONST]
 mov rcx, rax
 xor eax, eax
 call CONST
 mov rdi, rbp
 call CONST
LABEL8:
 add rsp, CONST
 mov r12d, CONST
 pop rbx
 mov eax, r12d
 pop rbp
 pop r12
 pop r13
 ret
